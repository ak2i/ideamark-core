{
  "name": "ideamark",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "ideamark": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "node -e \"console.log('lint placeholder')\""
  },
  "dependencies": {
    "@ideamark/core": "workspace:*",
    "commander": "^12.0.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
